{
  "name": "@botmock-api/utils",
  "version": "1.2.0",
  "description": "utilities for handling data from the Botmock API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest",
    "build": "tsc ./src/index.ts --outDir dist/",
    "prepublishOnly": "tsc ./src/index.ts --outDir dist/"
  },
  "keywords": [
    "botmock",
    "api",
    "typescript"
  ],
  "jest": {
    "preset": "ts-jest",
    "globals": {
      "ts-jest": {
        "tsConfig": "tsconfig.json"
      }
    }
  },
  "prettier": {
    "singleQuote": false,
    "printWidth": 80
  },
  "bugs": "https://github.com/Botmock/utils/issues",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Botmock/utils.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "24.0.15",
    "@types/node": "12.0.10",
    "jest": "24.8.0",
    "ts-jest": "^24.0.2",
    "ts-node": "8.3.0",
    "typescript": "3.5.2"
  }
}
